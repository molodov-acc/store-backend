# Store Backend

Backend проекта магазина с Node.js, Express, Prisma и PostgreSQL.  
Используется Docker для запуска API и базы данных.

---

## 🚀 Быстрый старт

### 1. Клонирование проекта

```bash
git clone <repo-url>
cd <repo-folder>
```

# Store Backend

Backend проекта магазина с Node.js, Express, Prisma и PostgreSQL.  
Используется Docker для запуска API и базы данных.

---

## 🚀 Быстрый старт

### 1. Клонирование проекта

```
git clone <repo-url>
cd <repo-folder>

```

## 2 Настройка переменных окружения

Скопируйте пример:

```
cp .env.example .env
```

## 3 Убедитесь, что Docker установлен, затем:

```
yarn start:docker
```

## 4 Сохранить изменения в коде и Prisma-моделях

#Если добавили или изменили модель, обязательно сохраните изменения в локальном репозитории.

```
yarn start:docker
```
